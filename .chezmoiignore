# Chezmoi profiles management
{{ if not (has "poste" .profiles) -}}
.config/mise/conf.d/poste.toml
{{ end -}}
{{ if not (has "mobile" .profiles) -}}
.config/mise/conf.d/mobile.toml
{{ end -}}

# Repository-only files
README.md
justfile

# Operational scripts invoked from the chezmoi source directory
scripts/**

# Temporary files
**/*.tmp
**/*.bak
**/*.log
**/*.swp

# VCS
.git/

# Dependencies
node_modules/


