# Chezmoi profiles management
{{ if not (has "poste" .profiles) -}}
.config/mise/conf.d/poste.toml
{{ end -}}
{{ if not (has "mobile" .profiles) -}}
.config/mise/conf.d/mobile.toml
{{ end -}}

# Temporary files
**/*.tmp
**/*.bak
**/*.log
**/*.swp

# VCS
.git/

# Dependencies
node_modules/
